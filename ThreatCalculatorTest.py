import json
import os
from util.ThreatCalculator import threatCalculator
import maxminddb


# class ThreatCalculatorTest():

#  @classmethod
#  def calculate(cls):
#      rateLimit = os.getenv('rateLimit')
#      separator = os.getenv('separator')
#      fields = rateLimit.split(separator)
#      for field in fields:
#          attribute = field.split(':')[0]
#          timeout = field.split(':')[1]
# print(threatCalculator(cls.log, attribute, timeout))


# if __name__ == "__main__":
# RateCalculatorTest.calculate()
# ThreatCalculatorTest.calculate()
